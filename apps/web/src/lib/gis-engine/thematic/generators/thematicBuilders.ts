import { LegendBuilder } from "../builders/LegendBuilder";
import { PaletteManager } from "../builders/PaletteManager";
import { StyleBuilder } from "../builders/StyleBuilder";

export const legendBuilder = new LegendBuilder();
export const paletteManager = new PaletteManager();
export const styleBuilder = new StyleBuilder();
